{
  "name": "solidhat",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "pnpm run -r build",
    "watch": "pnpm run -r --parallel watch",
    "format": "prettier --write '**/*.{ts,json,md,yml,yaml}'"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  }
}
